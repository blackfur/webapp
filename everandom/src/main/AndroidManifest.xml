<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.suicide.everandom"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
            android:icon="@drawable/tridactyl_100px"
            android:label="@string/app_name">
        <activity android:name=".InsertActivity"
        android:exported="true">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".EditorActivity" android:label="@string/app_name"/>
        <activity android:exported="true" android:name=".ReplicaActivity" android:label="@string/app_name"/>
        <activity android:exported="true" android:name=".RebornActivity" android:label="@string/app_name"/>
        <activity android:exported="true" android:name=".SearchActivity" android:label="@string/app_name"/>
    </application>

</manifest>